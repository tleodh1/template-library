window.Components={header(){const labels={PPT:"PPT",WORD:"WORD",EXCEL:"EXCEL",ETC:"기타양식"};const menu=Object.entries(TemplateData.categories).map(([k,v])=>`<div class="nav-item"><button class="nav-btn" data-filter="${k}">${labels[k]}</button><div class="mega">${v.map(x=>`<button data-category="${x}">${x}</button>`).join("")}</div></div>`).join("");return `<header class="site-header"><nav class="nav container"><a class="brand" href="./">FORME</a><div class="nav-menu">${menu}<div class="nav-item"><button class="nav-btn" data-filter="favorites">즐겨찾기</button></div></div></nav></header>`},footer(){return `<footer class="site-footer"><div class="container">FORME · Personal Template Library</div></footer>`},card(x,f){return `<article class="card"><button class="favorite${f.includes(x.id)?" active":""}" data-favorite="${x.id}">♥</button><div class="preview">${x.type}</div><div class="body"><div class="meta">${x.type} · ${x.category} · ${x.updated}</div><h3>${x.title}</h3><p>${x.description}</p></div></article>`}};